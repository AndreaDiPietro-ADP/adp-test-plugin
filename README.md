# adp-test-plugin
It's a WordPress basic Plugin that can be used as Boilerplate. Its implementation is based on the Singleton Pattern.

It adds:
- a post type
- an options' page
- a shortcode
- a custom metabox on the edit page
- a taxonomy
